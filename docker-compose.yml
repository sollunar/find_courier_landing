version: "3.8"

services:
        nginx-site:
                image: ${FINDCOURIER_IMAGE:?Set FINDCOURIER_IMAGE to a registry image reference}
                restart: always
                container_name: findcourier-site
                network_mode: host
                volumes:
                        - /var/lib/findcourier:/var/lib/findcourier
